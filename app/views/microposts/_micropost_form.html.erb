<%= form_for @new_post do |f| %>
    <%= render 'shared/error_message', :object => f.object %>
    <div class="field">
      <%= f.text_area :content %>
    </div>
    <div class="actions">
      <%= f.submit "Submit" %>
    </div>
<% end %>
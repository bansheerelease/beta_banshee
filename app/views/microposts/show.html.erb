<%= paginate @microposts %>
<% if @user == current_user%>
    <h1> The wall of <%= @user.full_name %></h1>
    <br>   Добавить сюда инфы
    <%= render 'micropost_form' %>
<% else %>
    <% if !@user.nil? %>
      <%= render 'relationships/follow_form' %>
    <% end %>
<% end %>

<% if !@microposts.empty? %>
    <table class="microposts" summary="User microposts">
      <%= render @microposts %>
    </table>
<% end %>


<table>
  <% if !@users.empty? %>
      <% @users.each do |user| %>
        <tr>
            <td> <%= user.email %> </td>
            <td>
              <% @user = user %>
              <% if !@user.nil? %>
                <%= render 'relationships/unfollow' %>
              <% end %>
            </td>
            <td> <%= link_to 'Show him','/user/' + user.id.to_s %></td>
        </tr>
      <% end %>
      <%= paginate @users %>
  <% end %>
</table>

